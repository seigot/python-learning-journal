### Introduction （約1分）
Hi, everyone. Today, I'm going to explain about my midterm project "the Weekend Library Closing Time Finder".  

This project aims to help students efficiently identify libraries that match their weekend schedule.  
Library includes Public and University library.  
In real-world situations, library hours vary between Saturday and Sunday, and some university libraries have access restrictions, such as ID requirements.  

### Problem Statement （約1分）  
Some libraries remain open as late as 7:00 PM, allowing students to plan their study sessions flexibly.  
However, studying with too much concentration can cause students to lose track of time and miss their next scheduled activities.  
The key solution of this system is to find libraries whose closing time is closest to but not later than the user’s preferred time.  
This allows users to students stay focused on studying while still managing their schedule effectively.  

### solutions (class diagram) （約1分）
Now, let me explain how I solve this problem with the class diagram.
The system consists of three main classes.  
The first class is for the Library. The base Library class defines PublicLibrary and UniversityLibrary using inheritance.  
The second class is searching for the optimal closing time.  
The system manages predefined library opening and closing time data, and searches for the best match based on user input.  
The third class is displaying the results. The class aims to show the 3 search results to the user.  

### solutions (activity diagram) （約1分）
This diagram shows the overall process of the system. There are three main steps.  
First, all library data is loaded into memory before user input.  
Second, the system waits for the user input to get their preferred library closing time and day, either Saturday or Sunday.  
Third, from the user's input, the system displays up to three recommended libraries with the closest closing times.  

From a data structure and algorithm perspective,   
merge sort is used to sort the library closing times, because it has an efficient worst-case time complexity of O(n log n).  
Binary search is used to find the closest closing time, since it has a time complexity of O(log n), which is very efficient.  
In addition, a queue is used to display the results, because it ensures that the first result is shown first in a FIFO order.  

### Code Demo (Part1.) (2min)
Next, I will briefly explain how my code works.  

The first one is Library class.  
This is the base class for the PublicLibrary and UniversityLibrary classes, inherit from the Library class.

The second one is LibraryFinderSystem.  
This class finds the best matching libraries by sorting and searching the data.  
 - load_libraries() method: Load library data into the system and initialize sorted structures.
 - merge_sort_libraries() method: Sort libraries by closing time using merge sort by using recursive call.
 - binary_search_closest() method: Find the index of the library with the closest closing time that does not exceed the target time.

The last one is LibraryResultQueue class.  
This class manages and displays the results in order using a queue.  
 - enqueue() and dequeue() method: 
 - display_results(): Display library search results in ranked order.

### Code Demo (Part1.)  Run (2min)
Now, let me run the program.  

First, I enter "sat" for Saturday, and "19:00" as the desired time.  
The system searches for libraries using binary search.  
Here are the result. You can see that the Milpitas Library is recommended, with a closing time of 19:00.  

Next, I will show another example.  
I enter "sun" and "22:00". Again, the system uses binary search to find the closest valid closing time.  
The result shows Santa Clara University Library and so on.  

Finally, I show about an error case.  
When I enter an invalid day, like "aaa", the system displays an error message like this.  
Also, if no library matches the requested time, it returns a error with error message like this.  
This means that the system handles edge cases properly.  

### Conclusion （約1分）
In conclusion.  
In this project, I designed and developed the Weekend Library Closing Time Finder system.  
This project help students efficiently identify libraries that match their weekend schedule.  
To achieve efficient performance, I applied appropriate data structures and algorithms, like class inheritance, merge sort, and binary search.  
For future improvements, the system could be extended by incorporating real-time library data updates and several ethical perspectives.  

Thank you for your attention.  
